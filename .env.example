# App
PORT=3000

# Database configuration for local dev (optional, tests use Testcontainers)
# You can use a single DATABASE_URL or individual PG* variables
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=postgres
